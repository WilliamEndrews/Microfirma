{
  "name": "@microfirma/iso-characters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@microfirma/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
